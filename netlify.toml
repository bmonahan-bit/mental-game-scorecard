[build]
  command = "npm install && ./node_modules/.bin/vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
```

Save with **Ctrl+S**, close Notepad, then in Command Prompt:
```
git add .
git commit -m "Fix vite path"
git push